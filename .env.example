# Postgres (prod / Netlify DB / Neon / Supabase). Leave unset to use embedded PGlite.
# DATABASE_URL=postgres://user:pass@host:5432/noagency
# Persist the embedded PGlite database on disk (unset = in-memory).
PGLITE_DIR=./.pglite
